# Dev/test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0.0,<9.0.0
pytest-cov>=5.0.0,<6.0.0
# Narrower range than other pins here: ruff's default lint rule set has
# changed between versions (see pyproject.toml), so this is deliberately
# tight to reduce the chance of CI silently picking up a newer ruff with
# different behavior than what was actually tested.
ruff>=0.14.0,<0.15.0
